// @ts-nocheck
/* eslint-disable */
/* This file was generated by Unframer for Framer project 7e5af5d66397a8a1 "Velt", do not edit manually */
"use client";

/**
 * @typedef Locale
 * string
 */

/**
 * @typedef {{
 * children?: React.ReactNode
 * locale?: Locale
 * style?: React.CSSProperties
 * className?: string
 * id?: string
 * ref?: any
 * width?: any
 * height?: any
 * layoutId?: string
 * variant?: 'Active' | 'Default' | 'Tablet' // Variant
 * activeIcon?: 'Presence' | 'Cursors' | 'Live Huddle' | 'Live Selection' | 'Flock Mode' | 'Sync Video Player' | 'Comments' | 'Recording' | 'Tag User' | 'Arrow' | 'Design' | 'Data' | 'Feedback' | 'Bug' | 'Contact' | 'Share Flows' | 'Presence / Active' | 'Cursors / Active' | 'Live Huddle / Active' | 'Live Selection / Active' | 'Flock Mode / Active' | 'Sync Video Player / Active' | 'Comments / Active' | 'Recording / Active' | 'Tag User / Active' | 'Arrow / Active' | 'Design / Active' | 'Data / Active' | 'Feedback / Active' | 'Bug / Active' | 'Contact / Active' | 'Share Flows / Active' // Active Icon
 * defaultIcon?: 'Presence' | 'Cursors' | 'Live Huddle' | 'Live Selection' | 'Flock Mode' | 'Sync Video Player' | 'Comments' | 'Recording' | 'Tag User' | 'Arrow' | 'Design' | 'Data' | 'Feedback' | 'Bug' | 'Contact' | 'Share Flows' | 'Presence / Active' | 'Cursors / Active' | 'Live Huddle / Active' | 'Live Selection / Active' | 'Flock Mode / Active' | 'Sync Video Player / Active' | 'Comments / Active' | 'Recording / Active' | 'Tag User / Active' | 'Arrow / Active' | 'Design / Active' | 'Data / Active' | 'Feedback / Active' | 'Bug / Active' | 'Contact / Active' | 'Share Flows / Active' // Default Icon
 * feature?: string // Feature
}} Props

 */
import { stdin_default } from "../chunks/chunk-HMO2NITU.js";
import "../chunks/chunk-HFMNAFHB.js";
import { routes } from "../chunks/chunk-K34BXH3K.js";
import "../chunks/chunk-WSFCRVEQ.js";

// virtual:nav/item
import { Fragment } from "react";
import { ContextProviders } from "unframer";
import { WithFramerBreakpoints } from "unframer";
import { jsx } from "react/jsx-runtime";
var locales = [];
var defaultResponsiveVariants = {
	base: "UXNLRLNVy",
};
/** @type {function(Props): any} */
function ComponentWithRoot({ locale, ...rest }) {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"7e5af5d66397a8a15ce8ab3401cf6b1aee707f00a906595a26b5cba8a8c88d97"
			}
			locale={locale}
			locales={locales}
		>
			{jsx(stdin_default, {
				...rest,
			})}
		</ContextProviders>
	);
}
/**
 * @type {import("unframer").UnframerBreakpoint}
 * Represents a responsive breakpoint for unframer.
 */

/**
 * @typedef VariantsMap
 * Partial record of UnframerBreakpoint to Props.variant, with a mandatory 'base' key.
 * { [key in UnframerBreakpoint]?: Props['variant'] } & { base: Props['variant'] }
 */

/**
 * Renders ItemFramerComponent for all breakpoints with a variants map. Variant prop is inferred per breakpoint.
 * @function
 * @param {Omit<Props, 'variant'> & {variants?: VariantsMap}} props
 * @returns {any}
 */
ComponentWithRoot.Responsive = ({ locale = "", ...rest }) => {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"7e5af5d66397a8a15ce8ab3401cf6b1aee707f00a906595a26b5cba8a8c88d97"
			}
			locale={locale}
			locales={locales}
		>
			<WithFramerBreakpoints
				Component={stdin_default}
				variants={defaultResponsiveVariants}
				{...rest}
			/>
		</ContextProviders>
	);
};
Object.assign(ComponentWithRoot, stdin_default);
var item_default = ComponentWithRoot;
export { item_default as default };
