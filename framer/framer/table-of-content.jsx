// @ts-nocheck
/* eslint-disable */
/* This file was generated by Unframer for Framer project 7e5af5d66397a8a1 "Velt", do not edit manually */
"use client";

/**
 * @typedef Locale
 * string
 */

/**
 * @typedef {{
 * children?: React.ReactNode
 * locale?: Locale
 * style?: React.CSSProperties
 * className?: string
 * id?: string
 * ref?: any
 * width?: any
 * height?: any
 * layoutId?: string
 * contentID?: string // Content ID
 * title?: string // Title
 * titleColor?: string // Title Color
 * maxHeadingDepth?: number // Max Heading Depth
 * titleFont?: 'Urbanist' | 'system-ui' | 'Arial' | 'Helvetica' | 'Times New Roman' | 'Georgia' | 'Verdana' | 'Roboto' | 'Open Sans' // Title Font
 * contentFont?: 'Urbanist' | 'system-ui' | 'Arial' | 'Helvetica' | 'Times New Roman' | 'Georgia' | 'Verdana' | 'Roboto' | 'Open Sans' // Content Font
 * titleFontSize?: number // Title Font Size
 * contentFontSize?: number // Content Font Size
 * activeColor?: string // Active Color
 * inactiveColor?: string // Inactive Color
 * hoverColor?: string // Hover Color
 * cursorType?: 'pointer' | 'default' // Cursor Type
 * insetMultiplier?: number // Inset Multiplier
 * itemPadding?: number // Item Padding
 * itemSpacing?: number // Item Spacing
 * fallbackMessage?: string // Fallback Message
}} Props

 */
import { routes } from "./chunks/chunk-K34BXH3K.js";
import "./chunks/chunk-WSFCRVEQ.js";

// virtual:table-of-content
import { Fragment } from "react";
import { ContextProviders } from "unframer";

// /:https://framerusercontent.com/modules/SFTItkP83N0wyxGSqT8X/YBh8B2jxc3zXFF859eHh/Table_of_Content.js
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useEffect, useState } from "react";
import { motion } from "unframer";
import { addPropertyControls, ControlType } from "unframer";
var __unframerWindow = typeof window !== "undefined" ? window : void 0;
function TableOfContents(props) {
	const {
		contentId,
		maxDepth = 3,
		fallbackMessage = "No headings found",
		// Title props
		title = "Contents",
		titleColor = "#000000",
		titleFontFamily = "Urbanist",
		contentFontFamily = "Urbanist",
		titleFontSize = 18,
		contentFontSize = 14,
		// Style props
		activeColor = "#2563eb",
		inactiveColor = "#374151",
		hoverColor = "#3b82f6",
		insetMultiplier = 16,
		itemPadding = 8,
		itemSpacing = 8,
		cursorType = "pointer",
		style,
	} = props;
	const [headings, setHeadings] = useState([]);
	const [activeId, setActiveId] = useState("");
	const [error, setError] = useState(null);
	const generateUniqueId = (text, level, index) => {
		const baseId = text
			.toLowerCase()
			.replace(/[^a-z0-9]+/g, "-")
			.replace(/(^-|-$)/g, "");
		return `toc-${level}-${baseId}-${index}`;
	};
	useEffect(() => {
		try {
			const contentElement = document.getElementById(contentId);
			if (!contentElement) {
				setError(`Content element with ID "${contentId}" not found`);
				return;
			}
			const headingElements = contentElement.querySelectorAll(
				Array.from(
					{
						length: maxDepth,
					},
					(_, i) => `h${i + 1}`,
				).join(","),
			);
			if (headingElements.length === 0) {
				setError(null);
				setHeadings([]);
				return;
			}
			const headingsData = Array.from(headingElements).map((heading, index) => {
				const level = parseInt(heading.tagName.charAt(1));
				const text = heading.textContent.trim();
				const id = heading.id || generateUniqueId(text, level, index);
				if (!heading.id) {
					heading.id = id;
				}
				return {
					id,
					text,
					level,
					index,
				};
			});
			setError(null);
			setHeadings(headingsData);
		} catch (err) {
			setError("Error processing headings: " + err.message);
			setHeadings([]);
		}
	}, [contentId, maxDepth]);
	useEffect(() => {
		if (headings.length === 0) return;
		const handleScroll = () => {
			const headingElements = headings.map((heading) =>
				document.getElementById(heading.id),
			);
			const currentHeading = headingElements.find((element) => {
				if (!element) return false;
				const rect = element.getBoundingClientRect();
				return rect.top >= 0 && rect.top <= __unframerWindow.innerHeight / 3;
			});
			if (currentHeading) {
				setActiveId(currentHeading.id);
			}
		};
		__unframerWindow.addEventListener("scroll", handleScroll);
		handleScroll();
		return () => __unframerWindow.removeEventListener("scroll", handleScroll);
	}, [headings]);
	const handleClick = (id) => {
		const element = document.getElementById(id);
		if (element) {
			element.scrollIntoView({
				behavior: "smooth",
			});
		}
	};
	if (error) {
		return (
			<div className={"w-64 p-4 bg-white rounded-lg shadow-lg"} style={style}>
				<p className={"text-red-600 text-sm"}>{error}</p>
			</div>
		);
	}
	if (headings.length === 0) {
		return (
			<div className={"w-64 p-4 bg-white rounded-lg shadow-lg"} style={style}>
				<p className={"text-gray-500 text-sm"}>{fallbackMessage}</p>
			</div>
		);
	}
	return (
		<nav
			className={"w-64 p-4 bg-white rounded-lg shadow-lg"}
			style={{
				...style,
			}}
		>
			<h2
				className={"text-lg font-semibold mb-4"}
				style={{
					color: titleColor,
					fontFamily: titleFontFamily,
					fontSize: `${titleFontSize}px`,
				}}
			>
				{title}
			</h2>
			<ul
				style={{
					gap: `${itemSpacing}px`,
				}}
				className={"flex flex-col"}
			>
				{headings.map((heading) => (
					<motion.li
						style={{
							paddingLeft: `${(heading.level - 1) * insetMultiplier}px`,
							padding: `${itemPadding}px`,
							cursor: cursorType,
							fontFamily: contentFontFamily,
							fontSize: `${contentFontSize}px`,
							lineHeight: "150%",
						}}
						initial={false}
						animate={{
							color: activeId === heading.id ? activeColor : inactiveColor,
						}}
						whileHover={{
							x: 4,
							color: hoverColor,
							transition: {
								duration: 0.2,
							},
						}}
					>
						<span
							onClick={() => handleClick(heading.id)}
							className={activeId === heading.id ? "font-medium" : ""}
						>
							{heading.text}
						</span>
					</motion.li>
				))}
			</ul>
		</nav>
	);
}
addPropertyControls(TableOfContents, {
	contentId: {
		type: ControlType.String,
		title: "Content ID",
		description: "ID of the content container element",
	},
	// Title Controls
	title: {
		type: ControlType.String,
		title: "Title",
		description: "Title text for the table of contents",
		defaultValue: "Contents",
	},
	titleColor: {
		type: ControlType.Color,
		title: "Title Color",
		description: "Color of the title text",
		defaultValue: "#000000",
	},
	maxDepth: {
		type: ControlType.Number,
		title: "Max Heading Depth",
		description: "Maximum heading level to include (1-6)",
		defaultValue: 3,
		min: 1,
		max: 6,
		step: 1,
	},
	titleFontFamily: {
		type: ControlType.Enum,
		title: "Title Font",
		description: "Font family for the title",
		defaultValue: "Urbanist",
		options: [
			"Urbanist",
			"system-ui",
			"Arial",
			"Helvetica",
			"Times New Roman",
			"Georgia",
			"Verdana",
			"Roboto",
			"Open Sans",
		],
	},
	contentFontFamily: {
		type: ControlType.Enum,
		title: "Content Font",
		description: "Font family for the table of contents items",
		defaultValue: "Urbanist",
		options: [
			"Urbanist",
			"system-ui",
			"Arial",
			"Helvetica",
			"Times New Roman",
			"Georgia",
			"Verdana",
			"Roboto",
			"Open Sans",
		],
	},
	titleFontSize: {
		type: ControlType.Number,
		title: "Title Font Size",
		description: "Font size for the title in pixels",
		defaultValue: 18,
		min: 10,
		max: 48,
		step: 1,
		unit: "px",
	},
	contentFontSize: {
		type: ControlType.Number,
		title: "Content Font Size",
		description: "Font size for the table of contents items in pixels",
		defaultValue: 14,
		min: 8,
		max: 32,
		step: 1,
		unit: "px",
	},
	// Style Controls
	activeColor: {
		type: ControlType.Color,
		title: "Active Color",
		description: "Color of the active heading",
		defaultValue: "#2563eb",
	},
	inactiveColor: {
		type: ControlType.Color,
		title: "Inactive Color",
		description: "Color of inactive headings",
		defaultValue: "#374151",
	},
	hoverColor: {
		type: ControlType.Color,
		title: "Hover Color",
		description: "Color when hovering over a heading",
		defaultValue: "#3b82f6",
	},
	cursorType: {
		type: ControlType.Enum,
		title: "Cursor Type",
		description: "Cursor style on hover",
		defaultValue: "pointer",
		options: ["pointer", "default"],
	},
	insetMultiplier: {
		type: ControlType.Number,
		title: "Inset Multiplier",
		description: "Multiplier for heading indentation",
		defaultValue: 16,
		min: 0,
		max: 48,
		step: 4,
		unit: "px",
	},
	itemPadding: {
		type: ControlType.Number,
		title: "Item Padding",
		description: "Padding around each item",
		defaultValue: 8,
		min: 0,
		max: 24,
		step: 2,
		unit: "px",
	},
	itemSpacing: {
		type: ControlType.Number,
		title: "Item Spacing",
		description: "Space between items",
		defaultValue: 8,
		min: 0,
		max: 24,
		step: 2,
		unit: "px",
	},
	fallbackMessage: {
		type: ControlType.String,
		title: "Fallback Message",
		description: "Message to show when no headings are found",
		defaultValue: "No headings found",
	},
});

// virtual:table-of-content
import { WithFramerBreakpoints } from "unframer";
import { jsx } from "react/jsx-runtime";
var locales = [];
var defaultResponsiveVariants = {};
/** @type {function(Props): any} */
function ComponentWithRoot({ locale, ...rest }) {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"7e5af5d66397a8a15ce8ab3401cf6b1aee707f00a906595a26b5cba8a8c88d97"
			}
			locale={locale}
			locales={locales}
		>
			<TableOfContents {...rest} />
		</ContextProviders>
	);
}
/**
 * @type {import("unframer").UnframerBreakpoint}
 * Represents a responsive breakpoint for unframer.
 */

/**
 * @typedef VariantsMap
 * Partial record of UnframerBreakpoint to Props.variant, with a mandatory 'base' key.
 * { [key in UnframerBreakpoint]?: Props['variant'] } & { base: Props['variant'] }
 */

/**
 * Renders TableOfContentFramerComponent for all breakpoints with a variants map. Variant prop is inferred per breakpoint.
 * @function
 * @param {Omit<Props, 'variant'> & {variants?: VariantsMap}} props
 * @returns {any}
 */
ComponentWithRoot.Responsive = ({ locale = "", ...rest }) => {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"7e5af5d66397a8a15ce8ab3401cf6b1aee707f00a906595a26b5cba8a8c88d97"
			}
			locale={locale}
			locales={locales}
		>
			<WithFramerBreakpoints
				Component={TableOfContents}
				variants={defaultResponsiveVariants}
				{...rest}
			/>
		</ContextProviders>
	);
};
Object.assign(ComponentWithRoot, TableOfContents);
var table_of_content_default = ComponentWithRoot;
export { table_of_content_default as default };
