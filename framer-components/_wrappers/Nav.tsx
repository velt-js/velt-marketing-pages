"use client";
// @ts-expect-error generated unframer JSX, no types
import Raw from "../2025/nav";

type Props = {
  padding?: string;
  className?: string;
  style?: React.CSSProperties;
};

export function Nav({ padding = "12px 80px 12px 80px", className, style }: Props) {
  return (
    <Raw.Responsive
      Z1Z606P2J={padding}
      className={className}
      style={{ width: "100%", ...style }}
    />
  );
}
