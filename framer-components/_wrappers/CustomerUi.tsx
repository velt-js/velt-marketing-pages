"use client";
// @ts-expect-error generated unframer JSX, no types
import Raw from "../2025/customer-ui";

type Props = {
  padding?: string;
  className?: string;
  style?: React.CSSProperties;
};

export function CustomerUi({ padding, className, style }: Props) {
  return (
    <Raw.Responsive
      BIBe7haSa={padding}
      className={className}
      style={{ width: "100%", ...style }}
    />
  );
}
